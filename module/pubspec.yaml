name: module
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  audio_service: ^0.18.0-beta.0
  just_audio: ^0.9.5
  path_provider: ^2.0.2
  sqflite: ^2.0.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: nct.library.flutter.module
    iosBundleIdentifier: nct.library.flutter.module
